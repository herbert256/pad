<?php

  // Fixture for the parm: case in expressions/references.php: a tag whose template reads its own
  // parameter back through an expression rather than as a tag, which is the other half of what
  // _tags/withparm.php covers. Returning TRUE keeps the tag itself from printing.

  return TRUE;

?>
