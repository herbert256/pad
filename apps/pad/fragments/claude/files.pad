<p>The <code>files</code> tag lists files in a directory with filtering options.</p>

<h2>List Files in Current Directory</h2>
<pre>{open}files 'fragments/claude', base='app'{close}
  {open}$file{close}
{open}/files{close}</pre>
<ul>
{files 'fragments/claude', base='app', rows=10}
  <li>{$file}</li>
{/files}
</ul>
<p><em>(showing first 10 files)</em></p>

<h2>Filter by Extension</h2>
<pre>{open}files 'fragments/claude', base='app', mask='*.pad'{close}
  {open}$file{close}
{open}/files{close}</pre>
<ul>
{files 'fragments/claude', base='app', mask='*.pad', rows=8}
  <li>{$file}</li>
{/files}
</ul>
<p><em>(showing first 8 .pad files)</em></p>

<h2>File Properties</h2>
<pre>{open}files 'fragments/claude', base='app', mask='*.php', rows=3{close}
  Path: {open}$path{close}
  File: {open}$file{close}
  Item: {open}$item{close}
  Ext: {open}$ext{close}
{open}/files{close}</pre>
<table>
  <tr><th>path</th><th>file</th><th>item</th><th>ext</th></tr>
{files 'fragments/claude', base='app', mask='*.php', rows=3}
  <tr><td>{$path}</td><td>{$file}</td><td>{$item}</td><td>{$ext}</td></tr>
{/files}
</table>

<h2>Only Files (No Directories)</h2>
<pre>{open}files '.', base='app', onlyFiles=TRUE, rows=5{close}</pre>
<ul>
{files '.', base='app', onlyFiles=TRUE, rows=5}
  <li>{$file}</li>
{/files}
</ul>

<h2>Only Directories</h2>
<pre>{open}files '.', base='app', onlyDirs=TRUE{close}</pre>
<ul>
{files '.', base='app', onlyDirs=TRUE, rows=10}
  <li>{$file}/</li>
{/files}
</ul>

<h2>Options Reference</h2>
<table>
  <tr><th>Option</th><th>Description</th></tr>
  <tr><td>dir / first param</td><td>Directory path</td></tr>
  <tr><td>mask</td><td>File pattern (e.g., *.txt)</td></tr>
  <tr><td>onlyFiles</td><td>Only return files</td></tr>
  <tr><td>onlyDirs</td><td>Only return directories</td></tr>
  <tr><td>recursive</td><td>Include subdirectories</td></tr>
  <tr><td>exclude</td><td>Exclusion pattern</td></tr>
  <tr><td>base</td><td>Base path (app, data, pad)</td></tr>
</table>
