<?php

  // Forwards to properties/count.php: how many occurrences the target level has - the
  // size of its data set, or the occurrence counter where that has run past it.

  return include PAD . "properties/$name.php";

?>