<?php

  // sort - sorts the sequence ascending by value, each value keeping its key. Flagged in
  // actions/merge/, so a parameter naming stores sorts those along with it.

  asort ( $pqResult );

?>