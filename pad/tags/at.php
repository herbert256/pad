<?php

  // {at "country.id='f0_325'@mondial"} looks a value up in a data set or sequence by way
  // of an @ expression, and returns what it points at. The at/ subsystem does the
  // searching.

  return padAtValue ( $padParm );

?>