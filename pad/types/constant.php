<?php

  // Type handler for a PHP constant ({constant:PHP_VERSION}, or a bare tag naming a defined
  // constant): returns the constant's value as the tag's value.

  return constant ( $padTag [$pad] );

?>