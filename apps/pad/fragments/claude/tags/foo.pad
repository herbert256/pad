<p>The <code>foo</code> tag is a test/placeholder tag.</p>

<h2>Basic Usage</h2>
<pre>{open}foo{close}
  Test content
{open}/foo{close}</pre>
{foo}
<p>Content inside foo tag</p>
{/foo}

<h2>Purpose</h2>
<p>The foo tag is typically used for:</p>
<ul>
  <li>Testing PAD syntax</li>
  <li>Placeholder during development</li>
  <li>Custom tag examples</li>
</ul>

<h2>Note</h2>
<p>Behavior may vary - check your app's _tags/foo.php if customized.</p>
