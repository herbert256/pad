<p>The <code>exists</code> tag checks if a file exists.</p>

<h2>Check File Exists</h2>
<pre>{open}exists APP . 'index.pad'{close}
  index.pad exists
{open}else{close}
  index.pad not found
{open}/exists{close}</pre>
{exists APP . 'index.pad'}
<p style="color:green">index.pad exists in APP directory</p>
{else}
<p style="color:red">index.pad not found</p>
{/exists}

<h2>Check Non-Existent File</h2>
<pre>{open}exists APP . 'nonexistent.xyz'{close}
  File exists
{open}else{close}
  File does not exist
{open}/exists{close}</pre>
{exists APP . 'nonexistent.xyz'}
<p style="color:green">File exists</p>
{else}
<p style="color:red">File does not exist</p>
{/exists}

<h2>Check Directory</h2>
<pre>{open}exists APP . 'fragments/claude'{close}
  claude directory exists
{open}/exists{close}</pre>
{exists APP . 'fragments/claude'}
<p style="color:green">claude directory exists</p>
{/exists}

<h2>Conditional Include</h2>
<pre>{open}exists APP . 'optional.pad'{close}
  {open}page 'optional'{close}
{open}else{close}
  Optional content not available
{open}/exists{close}</pre>
{exists APP . 'optional.pad'}
<p>Optional page would be included here</p>
{else}
<p><em>Optional content not available</em></p>
{/exists}

<h2>Check Config File</h2>
<pre>{open}exists APP . '_config/config.php'{close}
  Custom configuration loaded
{open}else{close}
  Using default configuration
{open}/exists{close}</pre>
{exists APP . '_config/config.php'}
<p>Custom configuration available</p>
{else}
<p>Using default configuration</p>
{/exists}

<h2>Use Cases</h2>
<ul>
  <li>Check if optional files exist before including</li>
  <li>Verify uploads or generated files</li>
  <li>Feature detection based on file presence</li>
  <li>Conditional loading of configurations</li>
</ul>
