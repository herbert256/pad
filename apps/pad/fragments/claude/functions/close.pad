<p>The <code>close</code> function outputs a literal closing brace <code>}</code>.</p>

<h2>Basic Usage</h2>
<pre>{open}close{close}</pre>
<p>Result: <b>{close}</b></p>

<h2>Document Closing Tags</h2>
<pre>{open}open{close}/if{open}close{close}</pre>
<p>Shows: <b>{open}/if{close}</b></p>

<pre>{open}open{close}/users{open}close{close}</pre>
<p>Shows: <b>{open}/users{close}</b></p>

<h2>Complete Tag Documentation</h2>
<pre>{open}open{close}if $x gt 0{open}close{close}
  positive
{open}open{close}/if{open}close{close}</pre>
<p>Shows:</p>
<code>{open}if $x gt 0{close}<br>
&nbsp;&nbsp;positive<br>
{open}/if{close}</code>

<h2>Note</h2>
<p>Always use <code>open</code> and <code>close</code> together when documenting PAD syntax. They escape the braces so PAD displays them literally instead of parsing them.</p>
