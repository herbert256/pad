<?php

  // Registry stub for the @start@ construct, one of the marker files this directory keeps so
  // tooling can test file_exists() for a construct name; the engine never includes them.
  // @start@ itself is detected by level/start.php and split off the level's base by
  // level/start_end/start1.php, so the text before it becomes leading content.

  return TRUE;

?>