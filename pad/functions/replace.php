<?php

  // Pipe function replace(search, replacement): replaces every occurrence of search in the
  // value. Case-sensitive; cut is the same thing with an empty replacement.

  return str_replace ( $parm[0], $parm[1], $value );

?>