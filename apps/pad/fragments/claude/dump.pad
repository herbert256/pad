<p>The <code>dump</code> tag outputs debug information.</p>

<h2>Syntax</h2>
<pre>{open}dump{close}
{open}dump 'message'{close}</pre>

<h2>What It Does</h2>
<p>The dump tag calls PAD's internal dump function to output debugging information. This typically includes:</p>
<ul>
  <li>Current variable state</li>
  <li>Tag processing context</li>
  <li>Level information</li>
  <li>Other debug data</li>
</ul>

<h2>Example Usage</h2>
<pre>{open}set testVar='Hello'{close}
{open}dump 'After setting testVar'{close}</pre>
<p>This would dump debug info with the message "After setting testVar".</p>

<h2>When to Use</h2>
<ul>
  <li>Debugging template issues</li>
  <li>Understanding variable scope</li>
  <li>Tracing execution flow</li>
  <li>Inspecting data structures</li>
</ul>

<h2>Output Location</h2>
<p>Dump output goes to:</p>
<ul>
  <li>The DATA/ directory as dump files</li>
  <li>Optionally to browser output</li>
  <li>Error logs (depending on configuration)</li>
</ul>

<h2>Sample Data</h2>
<p>Current sample data available:</p>
<pre>$sampleData = {open}$sampleData | json{close}</pre>

<h2>Warning</h2>
<p style="color:orange">The dump tag is for debugging only. Remove dump tags from production code as they can expose sensitive information and slow down execution.</p>

<h2>Note</h2>
<p>This demo page does not execute a live dump to avoid flooding the output. In real usage, dump would output detailed debug information.</p>
