<?php

  // Forwards to properties/firstFieldName.php: the name of the first field of the row the
  // target level is rendering, or '' when that row is empty.

  return include PAD . "properties/$name.php";

?>