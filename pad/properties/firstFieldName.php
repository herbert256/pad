<?php

  // The firstFieldName@tag property: the name of the first field of the row being
  // rendered, or '' when that row is empty. Companion of firstFieldValue.

  global $padCurrent;

  foreach ( $padCurrent [$padIdx] as $padK => $padV )
    return $padK;

  return '';

?>