<?php

  // Registry stub for the @end@ construct, one of the marker files this directory keeps so
  // tooling can test file_exists() for a construct name; the engine never includes them.
  // @end@ itself is detected by level/start.php and split off the level's base by
  // level/start_end/end1.php, so the text after it becomes trailing content.

  return TRUE;

?>