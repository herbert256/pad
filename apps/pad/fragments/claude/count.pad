<p>The <code>count</code> tag checks if an array has elements.</p>

<h2>Basic Count Check</h2>
<pre>{open}count items{close}
  Items array has elements
{open}/count{close}</pre>
{count items}
<p style="color:green">Items array has elements</p>
{/count}

<h2>Empty Array Check</h2>
<pre>{open}count emptyArray{close}
  Has elements
{open}else{close}
  Array is empty
{open}/count{close}</pre>
{count emptyArray}
<p>Has elements</p>
{else}
<p style="color:red">Array is empty</p>
{/count}

<h2>Count with List</h2>
<pre>{open}count users{close}
  &lt;p&gt;Users in the system:&lt;/p&gt;
  {open}users{close}
    - {open}$name{close}
  {open}/users{close}
{open}else{close}
  No users found
{open}/count{close}</pre>
{count users}
<p>Users in the system:</p>
<ul>
{users}
  <li>{$name}</li>
{/users}
</ul>
{else}
<p>No users found</p>
{/count}

<h2>Iteration Count Property</h2>
<pre>{open}items{close}
  Item {open}&amp;current{close} of {open}&amp;count{close}: {open}$name{close}
{open}/items{close}</pre>
<ul>
{items}
  <li>Item {&current} of {&count}: {$name}</li>
{/items}
</ul>
