<p>The <code>keep</code> tag marks values to keep during sequence filtering.</p>

<h2>Basic Keep</h2>
<pre>{open}sequence '1..10'{close}
  {open}if $value > 5{close}{open}keep{close}{open}/if{close}
{open}/sequence{close}</pre>
<p>This marks values greater than 5 to be kept.</p>

<h2>Keep Even Numbers</h2>
<pre>{open}sequence '1..10', push='nums'{close}
  {open}if {open}echo $value % 2{close} eq 0{close}{open}keep{close}{open}/if{close}
{open}/sequence{close}

{open}pull 'nums', keep{close}{open}$value{close} {open}/pull{close}</pre>
<p>Keep only even numbers from 1-10.</p>

<h2>Keep with Condition</h2>
<pre>{open}sequence '1..20'{close}
  {open}if $value >= 10{close}
    {open}if $value <= 15{close}
      {open}keep{close}
    {open}/if{close}
  {open}/if{close}
{open}/sequence{close}</pre>
<p>Keep only values between 10 and 15.</p>

<h2>Keep vs Remove</h2>
<table>
  <tr><th>Tag</th><th>Purpose</th></tr>
  <tr><td>keep</td><td>Mark values to include</td></tr>
  <tr><td>remove</td><td>Mark values to exclude</td></tr>
  <tr><td>flag</td><td>Generic marker</td></tr>
</table>

<h2>Use Case</h2>
<p>Use keep when you want to filter a sequence to only include values matching certain criteria. This is part of PAD's sequence filtering system.</p>

<h2>Note</h2>
<p>The keep tag works during sequence iteration to mark which values should be retained when the sequence is pulled with the keep option.</p>
