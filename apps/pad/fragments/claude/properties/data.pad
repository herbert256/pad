<p>The <code>@data</code> property returns the complete data array for the current level.</p>

<h2>Access Full Data</h2>
<pre>{open}users{close}
  {open}@first{close}
    Data has {open}@count{close} records
  {open}/@first{close}
{open}/users{close}</pre>

<p>Result:</p>
{users}
  {@first}
    Data has {@count} records
  {/@first}
{/users}

<h2>Note</h2>
<p>The <code>@data</code> property gives access to the raw underlying data array. This is primarily useful for advanced scenarios where you need to pass the complete dataset to another context.</p>
