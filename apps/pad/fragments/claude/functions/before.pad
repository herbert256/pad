<p>The <code>before</code> function returns everything before the first occurrence of a delimiter.</p>

<h2>Basic Usage</h2>
<pre>{open}echo $email | before('@'){close}</pre>
<p>Username: <b>{echo $email | before('@')}</b></p>

<h2>Extract Protocol</h2>
<pre>{open}echo 'https://example.com' | before('://'){close}</pre>
<p>Protocol: <b>{echo 'https://example.com' | before('://')}</b></p>

<h2>First Name</h2>
<pre>{open}echo 'John Smith' | before(' '){close}</pre>
<p>First name: <b>{echo 'John Smith' | before(' ')}</b></p>

<h2>Chained Examples</h2>
<pre>{open}echo $email | before('@') | upper{close}</pre>
<p>Username uppercase: <b>{echo $email | before('@') | upper}</b></p>

<pre>{open}echo $email | before('@') | replace('.', ' ') | capitalize{close}</pre>
<p>Formatted name: <b>{echo $email | before('@') | replace('.', ' ') | capitalize}</b></p>

<pre>{open}echo 'file.name.txt' | before('.') | upper{close}</pre>
<p>First part uppercase: <b>{echo 'file.name.txt' | before('.') | upper}</b></p>

<h2>Note</h2>
<p>If the delimiter is not found, the original string is returned unchanged.</p>
