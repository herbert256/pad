<?php

  // The key@tag property: the array key of the row being rendered, which for a keyed data
  // set is its name rather than its position - {key@config}: {$value}.

  global $padKey;

  return $padKey [$padIdx];

?>