<p>The <code>remaining</code> property outputs the number of items remaining.</p>

<pre>{open}users{close}
  {open}$name{close} - {open}property:remaining{close} more&lt;br&gt;
{open}/users{close}</pre>

<h3>Result:</h3>
{users}
  {$name} - {property:remaining} more<br>
{/users}
