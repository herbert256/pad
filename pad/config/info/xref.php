<?php

  // $padInfo selector 'xref': enables the cross-reference info type only
  // (info/types/xref/), which records which tags, functions, includes and files the request
  // actually used. Also switched on implicitly by inits/info.php for padReference requests.

  $padInfoXref = TRUE;

?>