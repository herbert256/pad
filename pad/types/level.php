<?php

  // Type handler for a level variable ({level:name}): returns the array held under that name by
  // the nearest enclosing level, searching outwards from the current one.

  return padGetLevelArray ( $padTag [$pad] );

?>