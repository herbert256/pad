{content 'seven'}
  {code}
    That is deep
  {/code}
{/content}
{# No stack dump here on purpose: one would put this machine's absolute paths and engine
   line numbers into the recorded answer, so the test could pass nowhere else and would
   break on any edit that moves a line in pad/. The chain of levels above is the thing
   under test, and it shows without one. #}