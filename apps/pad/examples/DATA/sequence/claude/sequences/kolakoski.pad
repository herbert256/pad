<p>The <code>kolakoski</code> sequence generates Kolakoski sequence.</p>

<h2>Kolakoski Sequence</h2>
<pre>{open}kolakoski rows=20{close}
  {open}$kolakoski{close}
{open}/kolakoski{close}</pre>
<p>Result: {kolakoski rows=20}{$kolakoski} {/kolakoski}</p>
