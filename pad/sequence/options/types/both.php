<?php

  // Marker file, never included: its existence is what makes 'both' a known sequence
  // option name - see exits/done.php, exits/info/options.php and exits/store/check.php.
  //
  // Modifier for the trim action: how many entries to cut off each end. Read into
  // $pqTrimBoth by actions/types/trim.php, applied before left= and right=, and filled in
  // from trim's own numeric parameter when it was written without a value.

  return TRUE;

?>