<p>The <code>between</code> function tests if a value is between two values (exclusive).</p>

<h2>Basic Usage</h2>
<pre>{open}if {open}echo 5 | between(1, 10){close}{close}In range{open}/if{close}</pre>
{if {echo 5 | between(1, 10)}}<p>Result: <b>5 is between 1 and 10</b></p>{/if}

<h2>Edge Cases (Exclusive)</h2>
<pre>{open}if not {open}echo 10 | between(1, 10){close}{close}At boundary{open}/if{close}</pre>
{if not {echo 10 | between(1, 10)}}<p>Edge: <b>10 is NOT between 1 and 10 (exclusive)</b></p>{/if}

<h2>Outside Range</h2>
<pre>{open}if not {open}echo 15 | between(1, 10){close}{close}Out of range{open}/if{close}</pre>
{if not {echo 15 | between(1, 10)}}<p>Result: <b>15 is NOT between 1 and 10</b></p>{/if}

<h2>String Comparison</h2>
<pre>{open}if {open}echo 'b' | between('a', 'c'){close}{close}In range{open}/if{close}</pre>
{if {echo 'b' | between('a', 'c')}}<p>Result: <b>'b' is between 'a' and 'c'</b></p>{/if}

<h2>Note</h2>
<p>Exclusive: boundaries are NOT included. Use <code>range</code> for inclusive comparison.</p>
