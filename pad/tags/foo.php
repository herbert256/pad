<?php

  // The built-in {foo} demo tag, there only so the manual's type-prefix page can show what
  // {pad:foo} resolves to next to {app:foo}, {php:foo}, {function:foo} and the rest.

  return "Foo tag from pad";

?>