<?php

  // Type handler for a named array in the current data context ({array:name}): returns the
  // array for the level to iterate, or [] when the name holds a scalar or nothing.

  return padArrayValue ( $padTag [$pad], 0 );

?>