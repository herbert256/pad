<?php

  // The {pull} tag: iterates a sequence that was stored earlier, optionally through actions
  // such as reverse, sort or first=3. Without a name it picks up the last pushed sequence.

  return include PQ . 'start/tags/pull.php';

?>