<p>The <code>sequence</code> tag generates a numeric sequence.</p>

<h2>Named Sequence</h2>
<pre>{open}sequence 5, name='i'{close}
  Number: {open}$i{close}
{open}/sequence{close}</pre>
<ul>
{sequence 5, name='i'}
  <li>Number: {$i}</li>
{/sequence}
</ul>

<h2>Range Sequence</h2>
<pre>{open}sequence '1..5', name='n'{close}
  {open}$n{close}
{open}/sequence{close}</pre>
<p>
{sequence '1..5', name='n'}
{$n}
{/sequence}
</p>

<h2>Sequence with Increment</h2>
<pre>{open}sequence '0..20', increment=5, name='n'{close}
  {open}$n{close}
{open}/sequence{close}</pre>
<p>
{sequence '0..20', increment=5, name='n'}
{$n}
{/sequence}
</p>

<h2>Nested Sequences (Table)</h2>
<pre>{open}sequence 3, name='row'{close}
  {open}sequence 4, name='col'{close}
    ({open}$row{close},{open}$col{close})
  {open}/sequence{close}
{open}/sequence{close}</pre>
<table border="1">
{sequence 3, name='row'}
  <tr>
  {sequence 4, name='col'}
    <td>({$row},{$col})</td>
  {/sequence}
  </tr>
{/sequence}
</table>
