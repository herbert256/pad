<?php

  // Placeholder for the reference: callback= is a real tag option, but it is not dispatched
  // through this directory - the iteration lifecycle consumes it in level/callback.php,
  // which runs callback/init.php, row.php and exit.php around the occurrences (or
  // callback/before.php for the before form). This file exists so the directory-driven
  // reference lists the option; nothing includes it.

?>
