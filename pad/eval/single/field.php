<?php

  // field: - the ordinary variable lookup, searching the current level and its parents for a
  // field of that name and returning '' when there is none or it is not scalar.

  return padFieldValue ($name);

?>