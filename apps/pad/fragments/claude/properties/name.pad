<p>The <code>@name</code> property returns the name of the current tag/level.</p>

<h2>Get Tag Name</h2>
<pre>{open}users{close}
  {open}@first{close}
    Processing tag: {open}@name{close}
  {open}/@first{close}
{open}/users{close}</pre>

<p>Result:</p>
{users}
  {@first}
    Processing tag: {@name}
  {/@first}
{/users}

<h2>Multiple Tags</h2>
<pre>{open}users{close}
  In: {open}@name{close}
{open}/users{close}

{open}colors{close}
  In: {open}@name{close}
{open}/colors{close}</pre>

<p>Result:</p>
{users}
  {@first}In: {@name}{/@first}
{/users}
<br>
{colors}
  {@first}In: {@name}{/@first}
{/colors}
