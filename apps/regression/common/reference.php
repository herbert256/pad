<?php

  // The globals the Xref link builders read; parts() answers an empty list these days, and
  // the count asserts exactly that.

  $first  = 1;
  $for    = 'demo';
  $xitem  = 'clock';
  $second = 'x';
  $go     = 1;

  $partsCount = count ( parts () );

  return TRUE;

?>