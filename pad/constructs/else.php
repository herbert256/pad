<?php

  // Registry stub for the @else@ construct, one of the marker files this directory keeps so
  // tooling can test file_exists() for a construct name; the engine never includes them.
  // @else@ itself is cut off by level/split.php, which hands the false branch to the level.

  return TRUE;

?>