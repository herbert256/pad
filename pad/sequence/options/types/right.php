<?php

  // Marker file, never included: its existence is what makes 'right' a known sequence
  // option name - see exits/done.php, exits/info/options.php and exits/store/check.php.
  //
  // Modifier for the trim action: how many entries to cut off the end of the sequence, on
  // top of whatever both= already took. Read into $pqTrimRight by actions/types/trim.php.

  return TRUE;

?>