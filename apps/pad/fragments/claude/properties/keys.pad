<p>The <code>@keys</code> property returns all keys with their values as iterable data.</p>

<h2>Iterate Over Keys</h2>
<pre>{open}config{close}
  {open}@first{close}
    {open}@keys{close}
      &lt;li&gt;{open}$name{close} = {open}$value{close}&lt;/li&gt;
    {open}/@keys{close}
  {open}/@first{close}
{open}/config{close}</pre>

<p>Result:</p>
<ul>
{config}
  {@first}
    {@keys}
      <li>{$name} = {$value}</li>
    {/@keys}
  {/@first}
{/config}
</ul>

<h2>Note</h2>
<p>The <code>@keys</code> property creates an iterable array where each item has <code>$name</code> (the key) and <code>$value</code> (the value). This is useful for displaying associative arrays dynamically.</p>
