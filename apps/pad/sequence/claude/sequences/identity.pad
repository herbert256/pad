<p>The <code>identity</code> sequence returns the same values (1, 2, 3, ...).</p>

<h2>Identity Sequence</h2>
<pre>{open}identity rows=10{close}
  {open}$identity{close}
{open}/identity{close}</pre>
<p>Result: {identity rows=10}{$identity} {/identity}</p>
