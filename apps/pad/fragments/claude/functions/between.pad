<p>The <code>between</code> function tests if a value is exclusively between two values (min &lt; value &lt; max).</p>

<h2>Basic Usage</h2>
<pre>{open}$sampleScore | between(0, 100){close}</pre>
<p>Score {$sampleScore} between 0 and 100 (exclusive): <b>{$sampleScore | between(0, 100)}</b></p>

<pre>{open}50 | between(0, 100){close}</pre>
<p>50 between 0 and 100: <b>{50 | between(0, 100)}</b></p>

<h2>Boundary Test</h2>
<pre>{open}0 | between(0, 100){close}</pre>
<p>0 between 0 and 100: <b>{0 | between(0, 100)}</b> (false - not exclusive)</p>

<pre>{open}100 | between(0, 100){close}</pre>
<p>100 between 0 and 100: <b>{100 | between(0, 100)}</b> (false - not exclusive)</p>

<h2>Age Ranges</h2>
<pre>{open}25 | between(17, 66){close}</pre>
<p>Age 25 is working age: <b>{25 | between(17, 66)}</b></p>

<pre>{open}17 | between(17, 66){close}</pre>
<p>Age 17 (boundary): <b>{17 | between(17, 66)}</b> (false - exclusive)</p>

<h2>Note</h2>
<p><code>between</code> uses exclusive comparison. For inclusive comparison, use <code>range</code>.</p>
