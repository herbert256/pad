<p>The <code>at</code> tag accesses a sequence value at a specific index.</p>

<h2>Access by Index</h2>
<pre>{open}sequence '10..20', push='nums'{close}{open}/sequence{close}

First (index 0): {open}at 'nums', 0{close}
Fifth (index 4): {open}at 'nums', 4{close}
Last (index -1): {open}at 'nums', -1{close}</pre>

{sequence '10..20', push='nums'}{/sequence}
<p>Sequence 10..20 stored as 'nums'</p>
<ul>
  <li>First (index 0): {at 'nums', 0}</li>
  <li>Fifth (index 4): {at 'nums', 4}</li>
</ul>

<h2>Negative Indexing</h2>
<pre>{open}sequence '1..10', push='data'{close}{open}/sequence{close}
Last: {open}at 'data', -1{close}
Second to last: {open}at 'data', -2{close}</pre>

{sequence '1..10', push='data'}{/sequence}
<p>Negative indices count from the end:</p>
<ul>
  <li>at 'data', -1 (last): {at 'data', -1}</li>
  <li>at 'data', -2 (second to last): {at 'data', -2}</li>
</ul>

<h2>Access Primes</h2>
<pre>{open}sequence prime, rows=10, push='primes'{close}{open}/sequence{close}
The 5th prime (index 4): {open}at 'primes', 4{close}</pre>

{sequence prime, rows=10, push='primes'}{/sequence}
<p>The 5th prime number is: {at 'primes', 4}</p>

<h2>Access Fibonacci</h2>
<pre>{open}sequence fibonacci, rows=15, push='fib'{close}{open}/sequence{close}
10th Fibonacci: {open}at 'fib', 9{close}</pre>

{sequence fibonacci, rows=15, push='fib'}{/sequence}
<p>10th Fibonacci number: {at 'fib', 9}</p>

<h2>Use Cases</h2>
<ul>
  <li>Get specific value from stored sequence</li>
  <li>Access first/last elements</li>
  <li>Random access to sequence values</li>
</ul>

<h2>Note</h2>
<p>The at tag requires the sequence to be stored first using the push option. Index is 0-based, negative indices count from end.</p>
