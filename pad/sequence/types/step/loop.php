<?php

  // Loop build for step, the strategy pqBuild() picks for this type: the loop value
  // unchanged. All the work is done by init.php, which sets the increment from the
  // parameter.

  return $pqLoop;

?>