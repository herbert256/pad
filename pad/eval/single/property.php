<?php

  // property: - returns a property of the nearest enclosing non-tag level, the values a tag
  // publishes about itself such as count, current, first and last.

  return padTagValue ( $name );

?>