<?php

  // Placeholder: the noError option has no handler of its own - level/no.php reads the name
  // directly and swallows an unresolved tag the way optional does. This file only exists so
  // the option is a known name; it used to be ignored outright, and the tag it was written
  // on leaked its own source into the page.

?>