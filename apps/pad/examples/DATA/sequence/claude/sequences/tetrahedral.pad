<p>The <code>tetrahedral</code> sequence generates tetrahedral numbers (pyramidal).</p>

<h2>Tetrahedral Sequence</h2>
<pre>{open}tetrahedral rows=10{close}
  {open}$tetrahedral{close}
{open}/tetrahedral{close}</pre>
<p>Result: {tetrahedral rows=10}{$tetrahedral} {/tetrahedral}</p>
