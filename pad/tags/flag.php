<?php

  // The {flag} tag: a sequence run in flag mode, where every value becomes 1 if it belongs to
  // the named sequence and 0 if it does not. The subsystem reads the mode off the tag name.

  return include PQ . 'start/tags/flag.php';

?>