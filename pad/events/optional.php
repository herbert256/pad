<?php

  // Records in the xref that the current tag used the generic 'optional' option, keyed by the
  // tag's type and name. Nothing includes this file at present.

  global $padInfoXref;

  if (  $padInfoXref )
    padInfoXref ( "options/generic/optional", $padType [$pad], $padTag [$pad] );

?>