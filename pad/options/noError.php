<?php

  // Placeholder: the noError option has no handler and this file is deliberately empty.
  //
  // No phase list names it and nothing else in the engine reads it either, so {tag noError} is
  // parsed and then ignored. The error suppression that does work is the optional option -
  // level/no.php calls options/optional.php - and the 'optional' pipe checked in level/var.php.

?>