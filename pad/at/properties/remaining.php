<?php

  // Forwards to properties/remaining.php: how many occurrences of the target level are
  // still to come, clamped at zero once the loop has run past its data.

  return include PAD . "properties/$name.php";

?>