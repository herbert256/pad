<p>The demo database through the credentials _common supplies: jim's phone is {$dbPhone}</p><?php // The one test that queries the database, so the demo credentials _common/_config // supplies are proven to work - the db tests in regression2 carry their own. $dbPhone = db ("field phone from staff where name = 'jim'");?>