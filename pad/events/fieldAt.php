<?php

  // Placeholder - padField() (pad/lib/field/field.php) fires this hook after padFieldAt()
  // has resolved a field name containing @ or ., but no instrumentation observes that
  // branch, so the file is deliberately empty. Its counterpart for plain names is
  // events/fieldClassic.php.

?>