<?php

  // Placeholder - the 'xref' mode has nothing to finish off, as padInfoXref appends to its
  // DATA/reference/ files while the page runs; even the output type is recorded at output
  // time (exits/output.php), because a file-writing page has already handed the request
  // back to 'web' by the time the exit runs. It exists because info/end/config.php ends
  // every enabled mode through a file of this name.

?>