<?php

  // A test with no .pad half at all. What a page's .php echoes is its output, which is why the
  // listing is taken from the .php files rather than from the templates.

  echo 'no template here';

?>