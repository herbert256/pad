<?php

  // Registry stub for the @tidy@ construct, one of the marker files this directory keeps so
  // tooling can test file_exists() for a construct name; the engine never includes them.
  // @tidy@ itself is looked for in the finished output by exits/tidy.php, where its presence
  // switches on HTML tidying for the response.

  return TRUE;

?>