<?php

  // Marker file, never included: its existence is what makes 'operation' a known sequence
  // option name - see exits/done.php, exits/info/options.php and exits/store/check.php.
  //
  // Nothing in the subsystem reads an operation= parameter - the play operations are named
  // directly, as make, keep, remove or flag - so this entry only reserves the name.

  return TRUE;

?>