<p>The <code>loop</code> sequence generates a simple counting loop.</p>

<h2>Basic Loop</h2>
<pre>{open}loop rows=10{close}
  {open}$loop{close}
{open}/loop{close}</pre>
<p>Result: {loop rows=10}{$loop} {/loop}</p>

<h2>Loop with From</h2>
<pre>{open}loop from=5, rows=10{close}</pre>
<p>From 5: {loop from=5, rows=10}{$loop} {/loop}</p>
