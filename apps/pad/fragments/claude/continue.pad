<p>The <code>continue</code> tag skips to the next iteration within a sequence.</p>

<h2>Basic Continue</h2>
<pre>{open}sequence '1..10'{close}
  {open}if $value eq 5{close}{open}continue{close}{open}/if{close}
  {open}$value{close}
{open}/sequence{close}</pre>
<p>Skip 5:</p>
<p>{sequence '1..10'}{if $value eq 5}{continue}{/if}{$value} {/sequence}</p>

<h2>Skip Even Numbers</h2>
<pre>{open}sequence '1..10'{close}
  {open}if {open}echo $value % 2{close} eq 0{close}{open}continue{close}{open}/if{close}
  {open}$value{close}
{open}/sequence{close}</pre>
<p>Odd only:</p>
<p>{sequence '1..10'}{if {echo $value % 2} eq 0}{continue}{/if}{$value} {/sequence}</p>

<h2>Skip Multiple Values</h2>
<pre>{open}sequence '1..10'{close}
  {open}if $value eq 3{close}{open}continue{close}{open}/if{close}
  {open}if $value eq 7{close}{open}continue{close}{open}/if{close}
  {open}$value{close}
{open}/sequence{close}</pre>
<p>Skip 3 and 7:</p>
<p>{sequence '1..10'}{if $value eq 3}{continue}{/if}{if $value eq 7}{continue}{/if}{$value} {/sequence}</p>

<h2>Use Case: Filter Display</h2>
<pre>{open}sequence '1..20'{close}
  {open}if $value < 5{close}{open}continue{close}{open}/if{close}
  {open}if $value > 15{close}{open}continue{close}{open}/if{close}
  {open}$value{close}
{open}/sequence{close}</pre>
<p>Show only 5-15:</p>
<p>{sequence '1..20'}{if $value < 5}{continue}{/if}{if $value > 15}{continue}{/if}{$value} {/sequence}</p>

<h2>Note</h2>
<p>The <code>continue</code> tag works within sequence loops to skip iterations based on conditions. It's similar to PHP's continue statement.</p>
