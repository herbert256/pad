<?php

  // The {keep} tag: a sequence run in keep mode, which throws away every value that is not a
  // member of the named sequence. The subsystem reads the mode off the tag name.

  return include PQ . 'start/tags/keep.php';

?>