<?php

  // The {until} tag: loops for as long as its condition stays FALSE. Shares its whole body
  // with tags/while.php, which reads $padTag [$pad] to decide which way round to test.

  return include PAD . 'tags/while.php';

?>