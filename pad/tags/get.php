<?php

  // The {get} tag: fetches another page of this application over HTTP with curl and returns
  // its output, escaped, as the tag's value. Body in start/get.php.

  return include PAD . 'start/get.php' ;

?>