<?php

  // Build strategy 'function': the type has a generator pqXxx($n) that turns the loop
  // value straight into a term. The loop iterator drives it and build/one.php makes the
  // call.

  include PQ . 'build/types/type/loop.php';

?>