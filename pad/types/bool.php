<?php

  // Type handler for a flag in the boolean store ({bool:name}, or a bare tag whose name is a
  // stored flag): returns the stored TRUE/FALSE, which level/flags.php turns into a hit or a
  // miss. Flags are put there by the toBool option.

  return $padBoolStore [$padTag [$pad]];

?>