<?php

  // Registry stub for the @content@ construct. This directory holds one file per
  // recognised @name@ marker, purely so tooling can test file_exists() for a construct
  // name; the engine never includes them. @content@ itself is handled by lib/content.php,
  // which wraps a tag's output around the marker when merging it into the level.

  return TRUE;

?>