<p>The <code>count</code> property outputs the total number of items.</p>

<pre>{open}users{close}
  {open}$name{close} ({open}property:current{close}/{open}property:count{close})&lt;br&gt;
{open}/users{close}</pre>

<h3>Result:</h3>
{users}
  {$name} ({property:current}/{property:count})<br>
{/users}
