<p>The <code>null</code> tag returns a null/empty value.</p>

<h2>Null in Comparison</h2>
<pre>{open}if {open}null{close} eq ''{close}
  Null equals empty string
{open}/if{close}</pre>
{if {null} eq ''}
<p style="color:green">Null equals empty string: true</p>
{/if}

<h2>Null vs Empty String</h2>
<pre>{open}set $empty = ''{close}
{open}if $empty eq {open}null{close}{close}
  Empty string equals null
{open}/if{close}</pre>
{set $empty = ''}
{if $empty eq {null}}
<p style="color:green">Empty string equals null: true</p>
{/if}

<h2>Note</h2>
<p>The null tag returns a null value, useful for comparisons and conditionals.</p>
