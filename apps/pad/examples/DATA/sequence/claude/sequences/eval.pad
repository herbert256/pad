<p>The <code>eval</code> sequence evaluates expressions.</p>

<h2>Eval Sequence</h2>
<pre>{open}eval '$n * $n', rows=10{close}
  {open}$eval{close}
{open}/eval{close}</pre>
<p>Squares via eval: {eval '$n * $n', rows=10}{$eval} {/eval}</p>
