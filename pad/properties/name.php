<?php

  // The name@tag property: the name level $padIdx is known by - its name= option, the
  // forced data name, or failing those the tag itself, as settled by level/name.php.

  global $padName;

  return $padName [$padIdx];

?>