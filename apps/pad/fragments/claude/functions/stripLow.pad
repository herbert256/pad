<p>The <code>stripLow</code> function strips low ASCII control characters (below 32).</p>

<h2>Basic Usage</h2>
<p>Removes control characters like tabs, newlines, and other non-printable characters.</p>

<h2>Clean Input</h2>
<pre>{open}'hello\tworld' | stripLow{close}</pre>
<p>Tab removed: <b>{'hello	world' | stripLow}</b></p>

<h2>Chained Examples</h2>
<pre>{open}'  text\twith\ttabs  ' | stripLow | trim | white{close}</pre>
<p>Fully cleaned: <b>{'  text	with	tabs  ' | stripLow | trim | white}</b></p>

<h2>Note</h2>
<p>Use <code>stripLow</code> to sanitize user input that may contain control characters. This is useful for cleaning data from external sources.</p>
