<?php

  // The redirect fixture: sends the caller on to the vars fixture. An action page, so the
  // walker leaves it out; the redirect scenario fetches it deliberately.

  padRedirect ( 'request/vars' );

?>