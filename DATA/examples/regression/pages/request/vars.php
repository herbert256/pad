<?php

  // The intake fixture: whatever the request carried - query or POST - arrives as a page
  // global before this line, so the defaults only fill what no request set.

  $a = $a ?? 'unset';
  $b = $b ?? 'unset';

?>
