<p>The <code>make</code> tag transforms values during sequence generation.</p>

<h2>Basic Make</h2>
<pre>{open}sequence '1..5'{close}
  {open}make{close}{open}echo $value * 2{close}{open}/make{close}
{open}/sequence{close}</pre>
<p>Double each value during iteration.</p>

<h2>Transform Values</h2>
<pre>{open}sequence '1..5', push='nums'{close}
  {open}make{close}{open}echo $value * $value{close}{open}/make{close}
{open}/sequence{close}

Squares: {open}pull 'nums', make{close}{open}$value{close} {open}/pull{close}</pre>
<p>Create squares of 1-5.</p>

<h2>Make with Format</h2>
<pre>{open}sequence '1..5'{close}
  {open}make{close}Item #{open}$value{close}{open}/make{close}
{open}/sequence{close}</pre>
<p>Transform numbers into formatted strings.</p>

<h2>Mathematical Transformations</h2>
<table>
  <tr><th>Transformation</th><th>Make Content</th></tr>
  <tr><td>Double</td><td>{open}echo $value * 2{close}</td></tr>
  <tr><td>Square</td><td>{open}echo $value * $value{close}</td></tr>
  <tr><td>Plus 10</td><td>{open}echo $value + 10{close}</td></tr>
  <tr><td>Percentage</td><td>{open}echo $value / 100{close}</td></tr>
</table>

<h2>Use Case: Data Transformation</h2>
<pre>{open}sequence '0..100', to=10, push='percentages'{close}
  {open}make{close}{open}$value{close}%{open}/make{close}
{open}/sequence{close}</pre>
<p>Transform numbers 0, 10, 20... into "0%", "10%", "20%"...</p>

<h2>Note</h2>
<p>The make tag allows you to transform sequence values during generation. The transformation is applied to each value in the sequence.</p>
