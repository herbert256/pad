<?php

  // What the at-type lines read: a global for @all, and one for the saved group to shadow.
  // The function line runs the catsum callback from _callbacks/, whose row phase totals the
  // rows into $t.

  $catAll   = 'found';
  $catSaved = 'outer';

?>