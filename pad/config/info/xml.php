<?php

  // $padInfo selector 'xml': enables the xml info type (info/types/xml/), which mirrors the
  // whole level/occurrence tree of the request as an XML document. The sub-flags add tag
  // parameters to that document and run it through tidy in compact form.

  $padInfoXml          = TRUE;

  $padInfoXmlParms     = TRUE;
  $padInfoXmlCompact   = TRUE;
  $padInfoXmlTidy      = TRUE;

?>