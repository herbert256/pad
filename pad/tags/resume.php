<?php

  // The {resume} tag: takes the sequence that was last pushed and carries on building it,
  // optionally through an action. Goes to the subsystem's own resume entry, not to tag.php.

  return include PQ . 'start/tags/resume.php';

?>