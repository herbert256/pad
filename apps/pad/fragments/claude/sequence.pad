<p>The <code>sequence</code> tag generates mathematical sequences.</p>

<h2>Basic Range</h2>
<pre>{open}sequence '1..5'{close}
  {open}$value{close}
{open}/sequence{close}</pre>
<p>Result: {sequence '1..5'}{$value} {/sequence}</p>

<h2>Range with Step</h2>
<pre>{open}sequence '0..10', to=2{close}
  {open}$value{close}
{open}/sequence{close}</pre>
<p>Result (step 2): {sequence '0..10', to=2}{$value} {/sequence}</p>

<h2>Countdown</h2>
<pre>{open}sequence '5..1'{close}
  {open}$value{close}
{open}/sequence{close}</pre>
<p>Result: {sequence '5..1'}{$value} {/sequence}</p>

<h2>Even Numbers</h2>
<pre>{open}sequence even, rows=10{close}
  {open}$value{close}
{open}/sequence{close}</pre>
<p>First 10 even numbers: {sequence even, rows=10}{$value} {/sequence}</p>

<h2>Prime Numbers</h2>
<pre>{open}sequence prime, rows=10{close}
  {open}$value{close}
{open}/sequence{close}</pre>
<p>First 10 primes: {sequence prime, rows=10}{$value} {/sequence}</p>

<h2>Fibonacci Sequence</h2>
<pre>{open}sequence fibonacci, rows=10{close}
  {open}$value{close}
{open}/sequence{close}</pre>
<p>First 10 Fibonacci: {sequence fibonacci, rows=10}{$value} {/sequence}</p>

<h2>Powers of 2</h2>
<pre>{open}sequence power, rows=8{close}
  {open}$value{close}
{open}/sequence{close}</pre>
<p>Powers of 2: {sequence power, rows=8}{$value} {/sequence}</p>

<h2>Building a Table</h2>
<pre>{open}sequence '1..5'{close}
  &lt;tr&gt;&lt;td&gt;Row {open}$value{close}&lt;/td&gt;&lt;/tr&gt;
{open}/sequence{close}</pre>
<table>
  <tr><th>Rows</th></tr>
  {sequence '1..5'}
  <tr><td>Row {$value}</td></tr>
  {/sequence}
</table>

<h2>Sequence Types</h2>
<table>
  <tr><th>Type</th><th>Description</th><th>Example</th></tr>
  <tr><td>'n..m'</td><td>Range from n to m</td><td>1..10</td></tr>
  <tr><td>even</td><td>Even numbers</td><td>2, 4, 6, 8...</td></tr>
  <tr><td>prime</td><td>Prime numbers</td><td>2, 3, 5, 7...</td></tr>
  <tr><td>fibonacci</td><td>Fibonacci sequence</td><td>1, 1, 2, 3, 5...</td></tr>
  <tr><td>power</td><td>Powers of 2</td><td>1, 2, 4, 8...</td></tr>
</table>

<h2>Options</h2>
<table>
  <tr><th>Option</th><th>Description</th></tr>
  <tr><td>rows</td><td>Limit number of results</td></tr>
  <tr><td>to</td><td>Step size</td></tr>
  <tr><td>push</td><td>Store sequence for later</td></tr>
</table>
