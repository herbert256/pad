<p>The <code>encodeHigh</code> function encodes high ASCII characters (above 127).</p>

<h2>Basic Usage</h2>
<pre>{open}echo 'Café résumé' | encodeHigh{close}</pre>
<p>Result: <b>{echo 'Café résumé' | encodeHigh}</b></p>

<h2>Special Characters</h2>
<pre>{open}echo 'naïve' | encodeHigh{close}</pre>
<p>Encoded: <b>{echo 'naïve' | encodeHigh}</b></p>

<h2>Chained Examples</h2>
<pre>{open}echo 'Über' | encodeHigh | upper{close}</pre>
<p>Encoded upper: <b>{echo 'Über' | encodeHigh | upper}</b></p>

<pre>{open}echo 'São Paulo' | trim | encodeHigh{close}</pre>
<p>Clean encoded: <b>{echo 'São Paulo' | trim | encodeHigh}</b></p>

<h2>Note</h2>
<p>Useful for ensuring ASCII compatibility in certain contexts.</p>
