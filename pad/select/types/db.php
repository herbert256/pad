<?php

  // Placeholder for the reference: db= is an option of a declared select table tag,
  // consumed by lib/select.php when it builds the query. This directory exists so the
  // directory-driven reference can list the select options; nothing includes its files.

  return TRUE;

?>
