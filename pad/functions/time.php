<?php

  // Pipe function time: alias of date - includes the date function file, which sees the same
  // $value, $parm and $count and whose return value becomes this one.

  return include PAD . 'functions/date.php';

?>