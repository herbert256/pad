<p>
  The fresh build clears the error dumps and the suite results, then runs the seven
  suites. The reference and the examples are <a href="http://localhost/pad/develop">develop</a>'s
  artifacts, harvested there and left standing between builds - the suites test against
  what stands. Thousands of fetches, a minute or two; the
  <a href="?index">overview</a> shows the outcome when it lands.
</p>

<p>
  <a href="?build&go=1">Fresh build</a> - there is no undo; everything above is
  regenerated from the applications themselves.
</p>