#!/usr/bin/env bash

git config --global user.email "herbert@groot.jebbink.nl"
git config --global user.name "Herbert Groot Jebbink"

git config --global credential.helper store

git clone https://github.com/Herbert256/pad.git