<p>The <code>stripLow</code> function removes control characters (ASCII 0-31).</p>

<h2>Basic Usage</h2>
<pre>{open}echo 'text with	tabs' | stripLow{close}</pre>
<p>Result: <b>{echo 'text with	tabs' | stripLow}</b></p>

<h2>Remove Control Chars</h2>
<p>Strips null bytes, tabs, newlines, and other control characters.</p>

<h2>Chained Examples</h2>
<pre>{open}echo 'dirty	input' | stripLow | trim{close}</pre>
<p>Clean: <b>{echo 'dirty	input' | stripLow | trim}</b></p>

<pre>{open}echo 'line1
line2' | stripLow | white{close}</pre>
<p>Single line: <b>{echo 'line1
line2' | stripLow | white}</b></p>

<h2>Note</h2>
<p>Use to sanitize input that might contain hidden control characters.</p>
