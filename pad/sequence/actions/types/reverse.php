<?php

  // reverse - reverses the order of the sequence, keys and all. Flagged in
  // actions/merge/, so a parameter naming stores reverses those along with it.

  $pqResult = array_reverse ( $pqResult, true );

?>