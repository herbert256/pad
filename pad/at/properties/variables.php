<?php

  // Forwards to properties/variables.php: the level variables declared on the target
  // level's tag ($var, not %var) as an iterable name/value array.

  return include PAD . "properties/$name.php";

?>