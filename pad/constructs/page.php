<?php

  // Registry stub for the @page@ construct, one of the marker files this directory keeps so
  // tooling can test file_exists() for a construct name; the engine never includes them.
  // @page@ itself is the wrapper placeholder: build/base.php nests the _inits.pad and
  // _exits.pad of every directory around it, and build/build.php substitutes the page.

  return TRUE;

?>