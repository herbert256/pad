<?php

  // Forwards to properties/keys.php: the target level's data set turned into iterable
  // name/value rows, one per key, with each value forced into PAD data form.

  return include PAD . "properties/$name.php";

?>