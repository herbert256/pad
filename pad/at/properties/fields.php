<?php

  // Forwards to properties/fields.php: the target level's current occurrence rebuilt as a
  // numbered list of name/value rows, so a template can loop over its fields.

  return include PAD . "properties/$name.php";

?>