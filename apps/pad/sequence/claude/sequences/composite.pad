<p>The <code>composite</code> sequence generates composite (non-prime) numbers.</p>

<h2>Basic Composite</h2>
<pre>{open}composite rows=15{close}
  {open}$composite{close}
{open}/composite{close}</pre>
<p>Result: {composite rows=15}{$composite} {/composite}</p>
