<?php

  if ( $padNull [$pad]  ) padXref ( 'flag', 'null',  $padNull [$pad]  );
  if ( $padElse [$pad]  ) padXref ( 'flag', 'else',  $padElse [$pad]  );
  if ( $padHit [$pad]   ) padXref ( 'flag', 'hit',   $padHit [$pad]   );
  if ( $padArray [$pad] ) padXref ( 'flag', 'array', $padArray [$pad] );

?>