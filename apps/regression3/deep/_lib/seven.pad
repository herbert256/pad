{content 'seven'}
  {code}
    That is deep
  {/code}
{/content}
{# The {padDumpStack} that used to print here made the recorded answer a page of this
   machine's absolute paths and engine line numbers, so the test could not pass anywhere
   else and broke on any edit that moved a line in pad/. The chain of levels above is the
   thing under test, and it shows without the dump. #}