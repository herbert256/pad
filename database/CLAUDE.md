# database/

SQL schema files for PAD's database tables.

## Files
- `pad.sql` - Main PAD tables schema
- `cache.sql` - Cache table schema
- `database.sql` - Database creation script

## Usage
These SQL files define the database structure used by PAD for caching and internal tracking. Run these scripts to set up the required database tables.
