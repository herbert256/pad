<p>The <code>done</code> property outputs the number of items completed.</p>

<pre>{open}users{close}
  {open}$name{close} - {open}property:done{close} done&lt;br&gt;
{open}/users{close}</pre>

<h3>Result:</h3>
{users}
  {$name} - {property:done} done<br>
{/users}
