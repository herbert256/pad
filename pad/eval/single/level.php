<?php

  // level: - returns the array a named tag holds at the nearest enclosing level, walking
  // $padCurrent outwards from the current level until one carries an array under that name.

  return padGetLevelArray ($name);

?>