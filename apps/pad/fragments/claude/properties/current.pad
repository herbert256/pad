<p>The <code>current</code> property outputs the current iteration number (1-based).</p>

<pre>{open}users{close}
  {open}property:current{close}. {open}$name{close}&lt;br&gt;
{open}/users{close}</pre>

<h3>Result:</h3>
{users}
  {property:current}. {$name}<br>
{/users}
