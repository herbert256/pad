<p>The <code>white</code> function normalizes whitespace (collapses multiple spaces to single space).</p>

<h2>Basic Usage</h2>
<pre>{open}echo $spacedText | white{close}</pre>
<p>Result: <b>{echo $spacedText | white}</b></p>

<h2>Multiple Spaces</h2>
<pre>{open}echo 'too    many     spaces' | white{close}</pre>
<p>Normalized: <b>{echo 'too    many     spaces' | white}</b></p>

<h2>Mixed Whitespace</h2>
<pre>{open}echo 'tabs	and   spaces' | white{close}</pre>
<p>Cleaned: <b>{echo 'tabs	and   spaces' | white}</b></p>

<h2>Chained Examples</h2>
<pre>{open}echo $spacedText | white | trim{close}</pre>
<p>Full cleanup: <b>{echo $spacedText | white | trim}</b></p>

<pre>{open}echo '  hello    world  ' | white | trim | capitalize{close}</pre>
<p>Clean and format: <b>{echo '  hello    world  ' | white | trim | capitalize}</b></p>

<pre>{open}echo 'Name:    John    Doe' | white | after(': '){close}</pre>
<p>Extract value: <b>{echo 'Name:    John    Doe' | white | after(': ')}</b></p>

<h2>Note</h2>
<p>Combine with <code>trim</code> for full whitespace normalization.</p>
