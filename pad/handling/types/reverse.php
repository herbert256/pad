<?php

  // Handles the reverse option: turns the tag's data set back to front. The keys are not
  // preserved, so the rows are renumbered from 0.

  $padData [$pad] = array_reverse ( $padData [$pad] );

?>