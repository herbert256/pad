<?php

  // Build strategy 'loop' for the loop sequence: the term is the loop value unchanged, so
  // the sequence is just the counter driven by from=, to= and increment= - or by the row
  // count init.php reads out of the parameter. The plain counting loop of the subsystem.

  return $pqLoop;

?>