<p>The <code>remove</code> tag marks values to exclude during sequence filtering.</p>

<h2>Basic Remove</h2>
<pre>{open}sequence '1..10'{close}
  {open}if $value eq 5{close}{open}remove{close}{open}/if{close}
{open}/sequence{close}</pre>
<p>This marks value 5 to be removed.</p>

<h2>Remove Multiples</h2>
<pre>{open}sequence '1..20', push='nums'{close}
  {open}if {open}echo $value % 3{close} eq 0{close}{open}remove{close}{open}/if{close}
{open}/sequence{close}

{open}pull 'nums', remove{close}{open}$value{close} {open}/pull{close}</pre>
<p>Remove multiples of 3 from 1-20.</p>

<h2>Remove Range</h2>
<pre>{open}sequence '1..10'{close}
  {open}if $value >= 4{close}
    {open}if $value <= 6{close}
      {open}remove{close}
    {open}/if{close}
  {open}/if{close}
{open}/sequence{close}</pre>
<p>Remove values 4, 5, and 6.</p>

<h2>Remove vs Keep</h2>
<table>
  <tr><th>Tag</th><th>Logic</th><th>Use when</th></tr>
  <tr><td>keep</td><td>Include matching</td><td>Few to keep</td></tr>
  <tr><td>remove</td><td>Exclude matching</td><td>Few to remove</td></tr>
</table>

<h2>Use Case: Filter Out Invalid</h2>
<pre>{open}sequence '1..100', push='data'{close}
  {open}if $value < 10{close}{open}remove{close}{open}/if{close}
  {open}if $value > 90{close}{open}remove{close}{open}/if{close}
{open}/sequence{close}</pre>
<p>Remove outliers (less than 10 or greater than 90).</p>

<h2>Note</h2>
<p>The remove tag is the opposite of keep. Use it to mark values that should be excluded from the final sequence.</p>
