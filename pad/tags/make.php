<?php

  // The {make} tag: a sequence run in make mode - the default mode - which generates the
  // named sequence itself instead of filtering values against it.

  return include PQ . 'start/tags/make.php';

?>