<p>The <code>emirp</code> sequence generates emirps (primes that are different primes when reversed).</p>

<h2>Emirp Sequence</h2>
<pre>{open}emirp rows=10{close}
  {open}$emirp{close}
{open}/emirp{close}</pre>
<p>Result: {emirp rows=10}{$emirp} {/emirp}</p>
