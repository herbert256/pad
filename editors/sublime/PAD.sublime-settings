{
    // Applies to files using the PAD syntax.
    // Pop the completion list right after typing {/ so open tags are offered.
    "auto_complete_triggers":
    [
        { "selector": "text.html.pad", "characters": "/" },
        { "selector": "text.html.pad meta.embedded.pad", "characters": ":$" }
    ]
}
