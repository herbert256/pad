<?php

  // Placeholder for the reference: negative is a parameter of the handling walk itself - it inverts the selection of every handler on the tag, read there rather than
  // dispatched as a handler of its own - handling/handling.php skips it by name. This file
  // only makes the option visible to the directory-driven reference.

  return TRUE;

?>
