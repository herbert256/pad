<p>The <code>trim</code> action removes elements from both ends.</p>

<h2>Trim Action</h2>
<pre>{open}sequence '1..10', push='nums'{close}
{open}pull:nums trim, both=2{close} {open}$sequence{close} {open}/pull:nums{close}</pre>
{sequence '1..10', push='nums'}
<p>Trimmed 2 from each end: {pull:nums trim, both=2} {$sequence} {/pull:nums}</p>

<h2>Trim Left Only</h2>
<pre>{open}pull:nums trim, left=3{close}</pre>
<p>Trimmed 3 from left: {pull:nums trim, left=3} {$sequence} {/pull:nums}</p>
